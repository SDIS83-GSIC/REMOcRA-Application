= Documentation des points d'entrée de l'API REMOcRA
:Author:    Atol C&D
:Email:     remocra-projet@atolcd.com
:Date:      01/09/2025
:Revision:  1.0.0
:imagesdir: images/
:experimental:
:icons: font
:toc:
:toclevels: 1
:numbered:

<<<
== Introduction générale

Cette documentation décrit tous les points d'entrée disponibles dans l'API REMOcRA v3.

La plupart des endpoints supportent la pagination via les paramètres :

*  `limit` correspond au nombre (maximum) d'éléments à retourner
*  `offset` correspond au nombre d'éléments à ignorer depuis le début, *à partir de 0*

== Authentification

L'API utilise l'authentification Bearer JWT. Tous les endpoints (sauf OpenAPI) nécessitent une authentification.

*Format d'en-tête :*
[source]
----
Authorization: Bearer <token>
----

*Droits requis :*

* `RECEVOIR` : Pour les opérations de lecture
* `TRANSMETTRE` : Pour les opérations d'écriture (création, modification, suppression)

== Référentiels Communs

*Base URL :* `/api/referentiel`

=== Types d'Organismes

==== `GET /typesOrganismes`
Retourne les types d'organismes susceptibles d'exploiter REMOcRA.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `TypeOrganisme` :

[source,json]
----
[
  {
    "typeOrganismeId": "uuid",
    "typeOrganismeActif": true,
    "typeOrganismeProtected": false,
    "typeOrganismeCode": "string",
    "typeOrganismeLibelle": "string",
    "typeOrganismeParentId": "uuid (optionnel)",
    "typeOrganismeDroitApi": ["RECEVOIR", "TRANSMETTRE"]
  }
]
----

=== Communes

==== `GET /communes`
Retourne la liste des communes.

*Paramètres de requête :*

- `codeInsee` (optionnel) : Code INSEE de la commune
- `libelle` (optionnel) : Tout ou partie du nom de la commune
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Commune` :

[source,json]
----
[
  {
    "communeId": "uuid",
    "communeLibelle": "string",
    "communeCodeInsee": "string",
    "communeCodePostal": "string",
    "communeGeometrie": {
      "type": "Polygon",
      "coordinates": [...]
    },
    "communePprif": false,
    "communeCode": "string (optionnel)"
  }
]
----

=== Voies

==== `GET /voies/{codeInsee}`
Retourne les voies d'une commune donnée.

*Paramètres de chemin :*

- `codeInsee` (requis) : Code INSEE de la commune

*Paramètres de requête :*

- `libelle` (optionnel) : Tout ou partie du nom de la voie
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Voie` :

[source,json]
----
[
  {
    "voieId": "uuid",
    "voieLibelle": "string",
    "voieGeometrie": {
      "type": "LineString",
      "coordinates": [...]
    },
    "voieCommuneId": "uuid"
  }
]
----

=== Organismes

==== `GET /organismes`
Retourne les organismes susceptibles d'exploiter REMOcRA (utilisateurs nommés avec accès à l'interface applicative ou exploitation de l'API).

*Paramètres de requête :*

- `codeTypeOrganisme` (optionnel) : Code du type de l'organisme
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Organisme` :

[source,json]
----
[
  {
    "organismeId": "uuid",
    "organismeActif": true,
    "organismeCode": "string",
    "organismeLibelle": "string",
    "organismeEmailContact": "string (optionnel)",
    "organismeProfilOrganismeId": "uuid",
    "organismeTypeOrganismeId": "uuid",
    "organismeZoneIntegrationId": "uuid",
    "organismeParentId": "uuid (optionnel)"
  }
]
----

== DECI - Points d'Eau Incendie

*Base URL :* `/api/deci/pei`

=== Liste des PEI

==== `GET /`
Retourne la liste des PEI en lien avec l'organisme courant. La structure de donnée retournée est conforme au modèle de donnée minimal défini par l'AFIGEO.

*Paramètres de requête :*

- `insee` (optionnel) : Numéro INSEE de la commune où se trouve le PEI
- `type` (optionnel) : Type du PEI ('PIBI' ou 'PENA')
- `codeNature` (optionnel) : Nature du PEI
- `codeNatureDECI` (optionnel) : Nature DECI ('PRIVE', 'PUBLIC', 'CONVENTIONNE', 'ICPE', 'ICPE_CONVENTIONNE')
- `limit` (optionnel, max 200, défaut 200) : Nombre maximum de résultats à retourner
- `start` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ModeleMinimalPei` conformes au modèle AFIGEO :

[source,json]
----
[
  {
    "peiId": "uuid",
    "peiNumeroComplet": "string",
    "typePeiNexSis": "string (optionnel)",
    "isDisponible": true,
    "geometrie": {
      "type": "Point",
      "coordinates": [longitude, latitude]
    },
    "codeInsee": "string",
    "communeLibelle": "string",
    "idGestion": "string (optionnel)",
    "nomGest": "string (optionnel)",
    "peiNumeroInterne": "string",
    "sourcePei": "string (optionnel)",
    "diametre": 100,
    "pibiDiametreCanalisation": 150,
    "natureLibelle": "string (optionnel)",
    "natureDeci": "string",
    "site": "string (optionnel)",
    "adresse": "string",
    "pibiPressionDynamique": 4.5,
    "pibiPression": 6.0,
    "pibiDebit": 60,
    "penaVolumeConstate": 120000,
    "instantChangementDispo": "2024-12-25T14:30:00.000Z",
    "dateMiseEnService": "2024-01-15T00:00:00.000Z",
    "dateMiseAJour": "2024-12-20T10:30:00.000Z",
    "dateDernierControleTechnique": "2024-11-15T09:00:00.000Z",
    "dateDerniereRop": "2024-10-10T08:00:00.000Z",
    "precision": "string (optionnel)",
    "isNonConforme": false,
    "isAccessibleHbe": true
  }
]
----

==== `GET /{numeroComplet}`
Retourne les informations communes à tout type de PEI d'un PEI spécifique.

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro du PEI

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Objet `ModeleMinimalPei` (voir structure ci-dessus)

=== Caractéristiques du PEI

==== `GET /{numeroComplet}/caracteristiques`
Retourne les caractéristiques techniques propres au PEI et à son type (PIBI ou PENA).

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro du PEI

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `PeiCaracteristique` :

[source,json]
----
[
  {
    "caracteristique": "string (nom de la caractéristique)",
    "value": "valeur (type variable selon la caractéristique)"
  }
]
----

==== `PUT /{numeroComplet}/pibi-caracteristiques`
Modifie les caractéristiques techniques propres au PIBI.

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro du PEI

*Droit requis :* `TRANSMETTRE`

*Structure de requête :*

Objet `ApiPibiFormData` :

[source,json]
----
{
  "codeDiametre": "string (optionnel)",
  "diametreCanalisation": 150,
  "peiJumele": "string (optionnel)",
  "inviolabilite": true,
  "renversable": false,
  "codeMarque": "string (optionnel)",
  "codeModele": "string (optionnel)",
  "anneeFabrication": 2020,
  "codeTypeReseau": "string (optionnel)",
  "codeTypeCanalisation": "string (optionnel)",
  "reseauSurpresse": false,
  "reseauAdditive": false
}
----

*Structure de réponse :*

Objet `ModeleMinimalPei` mis à jour (voir structure ci-dessus)

==== `PUT /{numeroComplet}/pena-caracteristiques`
Modifie les caractéristiques techniques propres au PENA.

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro du PEI

*Droit requis :* `TRANSMETTRE`

*Structure de requête :*

Objet `ApiPenaFormData` :

[source,json]
----
{
  "capaciteIllimitee": false,
  "capaciteIncertaine": false,
  "capacite": 120000,
  "quantiteAppoint": 30.5,
  "codeMateriau": "string (optionnel)",
  "equipeHBE": true
}
----

*Structure de réponse :*

Objet `ModeleMinimalPei` mis à jour (voir structure ci-dessus)

=== Différentiel des modifications

==== `GET /diff`
Liste des PEI ayant subi une modification (ajout, modification ou suppression) postérieure au moment passé en paramètre.

*Paramètres de requête :*

- `moment` (requis) : Moment à partir duquel retourner les résultats, format YYYY-MM-DD hh:mm

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `PeiDiff` :

[source,json]
----
[
  {
    "peiId": "uuid",
    "numeroComplet": "string",
    "momentModification": "2024-12-25T14:30:00.000Z",
    "auteurModification": "string (optionnel)",
    "auteur": {
      "type": "string",
      "details": "object"
    },
    "typeOperation": "INSERT | UPDATE | DELETE",
    "typeObjet": "string"
  }
]
----

== DECI - Référentiels

=== Référentiels Communs DECI

*Base URL :* `/api/deci/referentiel`

==== `GET /naturesDECI`
Retourne les types de DECI applicables sur les PEI (publique, privée, privée sous convention, ICPE, ICPE conventionnée).

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `NatureDeci` :

[source,json]
----
[
  {
    "natureDeciId": "uuid",
    "natureDeciActif": true,
    "natureDeciCode": "string",
    "natureDeciLibelle": "string",
    "natureDeciProtected": false
  }
]
----

==== `GET /niveaux`
Retourne les valeurs de positionnement par rapport au sol possibles pour un PEI.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Niveau` :

[source,json]
----
[
  {
    "niveauId": "uuid",
    "niveauActif": true,
    "niveauCode": "string",
    "niveauLibelle": "string",
    "niveauProtected": false
  }
]
----

==== `GET /domaines`
Retourne les natures domaniales des terrains sur lesquels les PEI sont localisés.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Domaine` :

[source,json]
----
[
  {
    "domaineId": "uuid",
    "domaineActif": true,
    "domaineCode": "string",
    "domaineLibelle": "string"
  }
]
----

==== `GET /typesVisites`
Retourne les types de visites possibles sur les PEI.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau de valeurs énumérées (TypeVisite) : `["CTP", "ROI", "ROP", "NP", ...]`

=== Référentiels PIBI

*Base URL :* `/api/deci/referentiel/pibi`

==== `GET /naturesPEI`
Retourne les natures de PEI possibles pour les PEI de type PIBI.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Nature` (filtrés pour PIBI) :

[source,json]
----
[
  {
    "natureId": "uuid",
    "natureActif": true,
    "natureCode": "string",
    "natureLibelle": "string",
    "natureTypePei": "PIBI",
    "natureProtected": false,
    "natureTypePeiNexsis": "string (optionnel)"
  }
]
----

==== `GET /diametres/{natureCode}`
Retourne les diamètres de demi-raccord possibles pour une nature de PIBI.

*Paramètres de chemin :*

- `natureCode` (requis) : Code de nature PIBI

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ApiDiametreCodeLibelle` :

[source,json]
----
[
  {
    "diametreCode": "string",
    "diametreLibelle": "string"
  }
]
----

==== `GET /{natureCode}/naturesAnomalies`
Retourne les types d'anomalies pouvant être constatées pour une nature de PIBI et un type de visite spécifiques.

*Paramètres de chemin :*

- `natureCode` (requis) : Nature du PIBI

*Paramètres de requête :*

- `typeVisite` (optionnel) : Type de la visite
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ApiAnomalieWithNature` :

[source,json]
----
[
  {
    "anomalieCode": "string",
    "anomalieLibelle": "string",
    "poidsAnomalieValIndispoTerrestre": 5,
    "listTypeVisite": ["CTP", "ROI"]
  }
]
----

==== `GET /marques`
Retourne les marques susceptibles d'équiper le parc de PIBI.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `MarquePibi` :

[source,json]
----
[
  {
    "marquePibiId": "uuid",
    "marquePibiActif": true,
    "marquePibiCode": "string",
    "marquePibiLibelle": "string"
  }
]
----

==== `GET /modeles`
Retourne les modèles susceptibles d'équiper le parc de PIBI.

*Paramètres de requête :*

- `codeMarque` (optionnel) : Code de la marque
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ModelePibi` :

[source,json]
----
[
  {
    "modelePibiId": "uuid",
    "modelePibiActif": true,
    "modelePibiCode": "string",
    "modelePibiLibelle": "string",
    "modelePibiMarqueId": "uuid"
  }
]
----

==== `GET /typeReseau`
Retourne les types de réseau.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `TypeReseau` :

[source,json]
----
[
  {
    "typeReseauId": "uuid",
    "typeReseauActif": true,
    "typeReseauCode": "string",
    "typeReseauLibelle": "string"
  }
]
----

==== `GET /typeCanalisation`
Retourne les types de canalisation.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `TypeCanalisation` :

[source,json]
----
[
  {
    "typeCanalisationId": "uuid",
    "typeCanalisationActif": true,
    "typeCanalisationCode": "string",
    "typeCanalisationLibelle": "string"
  }
]
----

=== Référentiels PENA

*Base URL :* `/api/deci/referentiel/pena`

==== `GET /naturesPEI`
Retourne les natures de PEI possibles pour les PEI de type PENA.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Nature` (filtrés pour PENA) :

[source,json]
----
[
  {
    "natureId": "uuid",
    "natureActif": true,
    "natureCode": "string",
    "natureLibelle": "string",
    "natureTypePei": "PENA",
    "natureProtected": false,
    "natureTypePeiNexsis": "string (optionnel)"
  }
]
----

==== `GET /materiaux`
Retourne les matériaux possibles pour les PEI de type PENA.

*Paramètres de requête :*

- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `Materiau` :

[source,json]
----
[
  {
    "materiauId": "uuid",
    "materiauActif": true,
    "materiauCode": "string",
    "materiauLibelle": "string"
  }
]
----

==== `GET /{natureCode}/naturesAnomalies`
Retourne les types d'anomalies pouvant être constatées pour une nature de PENA et un type de visite spécifiques.

*Paramètres de chemin :*

- `natureCode` (requis) : Nature du PENA

*Paramètres de requête :*

- `typeVisite` (optionnel) : Type de la visite
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ApiAnomalieWithNature` (voir format dans section PIBI)

== DECI - Visites

*Base URL :* `/api/deci/pei/{numeroComplet}/visites`

=== Liste des visites d'un PEI

==== `GET /`
Retourne les visites associées à un PEI. Une visite correspond à une séance terrain auprès d'un PEI à une date donnée dans un contexte périodique (contrôle technique périodique, reconnaissance opérationnelle périodique) ou non (reconnaissance initiale, intervention).

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro complet du PEI

*Paramètres de requête :*

- `codeTypeVisite` (optionnel) : Code du type de visite
- `moment` (optionnel) : Moment à partir duquel retourner les résultats, format YYYY-MM-DD hh:mm
- `derniereOnly` (optionnel) : Renvoyer uniquement la dernière visite (boolean)
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ApiVisite` :

[source,json]
----
[
  {
    "visiteId": "string",
    "moment": "2024-12-25T14:30:00.000Z",
    "visiteTypeVisite": "CTP",
    "anomalies": ["ANOM_001", "ANOM_002"]
  }
]
----

=== Ajouter une visite

==== `POST /`
Ajoute une visite à un PEI en précisant le type de visite, une liste de points contrôlés et une liste d'anomalies éventuellement constatées. Des mesures de débit et de pression peuvent également être transmises dans le cas d'un "Contrôle Technique Périodique".

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro complet du PEI

*Droit requis :* `TRANSMETTRE`

*Structure de requête :*

Objet `ApiVisiteFormData` :

[source,json]
----
{
  "date": "2024-12-25",
  "typeVisite": "CTP",
  "agent1": "Dupont Jean",
  "agent2": "Martin Paul",
  "anomaliesControlees": ["ANOM_001", "ANOM_002"],
  "anomaliesConstatees": ["ANOM_003"],
  "debit": 60,
  "pression": 6.0,
  "pressionDynamique": 4.5,
  "observations": "RAS"
}
----

*Réponse :* Code HTTP 201 (Créée avec succès)

=== Détails d'une visite

==== `GET /{idVisite}`
Retourne l'information détaillée d'une visite spécifique dont les éventuelles informations de débit et pressions.

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro complet du PEI
- `idVisite` (requis) : Identifiant de la visite

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Objet `ApiVisiteSpecifique` :

[source,json]
----
{
  "moment": "2024-12-25T14:30:00.000Z",
  "visiteId": "uuid",
  "typeVisite": "CTP",
  "agent1": "Dupont Jean",
  "agent2": "Martin Paul",
  "debit": 60,
  "pression": 6.0,
  "pressionDyn": 4.5,
  "ctrlDebitPression": true,
  "anomaliesConstatees": ["ANOM_001", "ANOM_002"],
  "anomaliesControlees": ["ANOM_003"],
  "observations": "RAS"
}
----

=== Modifier une visite

==== `PUT /{idVisite}`
Modifie les informations relatives à une visite spécifique.

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro complet du PEI
- `idVisite` (requis) : Identifiant de la visite

*Droit requis :* `TRANSMETTRE`

*Structure de requête :*

Objet `ApiUpdateVisiteFormData` :

[source,json]
----
{
  "agent1": "Dupont Jean",
  "agent2": "Martin Paul",
  "anomaliesControlees": ["ANOM_001", "ANOM_002"],
  "anomaliesConstatees": ["ANOM_003"],
  "debit": 60,
  "pression": 6.0,
  "pressionDynamique": 4.5,
  "observations": "RAS"
}
----

*Structure de réponse :*

Objet `ApiVisiteSpecifique` mis à jour (voir structure ci-dessus)

=== Supprimer une visite

==== `DELETE /{idVisite}`
Supprime une visite spécifique.

*Paramètres de chemin :*

- `numeroComplet` (requis) : Numéro complet du PEI
- `idVisite` (requis) : Identifiant de la visite

*Droit requis :* `TRANSMETTRE`

*Réponse :* Code HTTP 200 (Succès)

== DECI - Indisponibilités Temporaires

*Base URL :* `/api/deci/indispoTemporaire`

=== Liste des indisponibilités temporaires

==== `GET /`
Retourne la liste des indisponibilités temporaires de REMOcRA.

*Paramètres de requête :*

- `organismeApi` (optionnel) : Code de l'organisme API étant à l'origine de l'indisponibilité temporaire
- `numeroComplet` (optionnel) : Ne renvoie que les indisponibilités temporaires liées à ce PEI
- `statut` (optionnel) : Code du statut de l'indisponibilité temporaire ('EN_COURS', 'PLANIFIEE' ou 'TERMINEE')
- `limit` (optionnel) : Nombre maximum de résultats à retourner
- `offset` (optionnel) : Retourne les informations à partir de la n-ième ligne

*Droit requis :* `RECEVOIR`

*Structure de réponse :*

Tableau d'objets `ApiIndispoTemporaire` :

[source,json]
----
[
  {
    "indisponibiliteTemporaireId": "uuid",
    "indisponibiliteTemporaireDateDebut": "2024-12-25T14:30:00.000Z",
    "indisponibiliteTemporaireDateFin": "2024-12-26T14:30:00.000Z",
    "indisponibiliteTemporaireMotif": "Travaux sur le réseau",
    "indisponibiliteTemporaireObservation": "Remplacement de canalisation",
    "indisponibiliteTemporaireBasculeAutoIndisponible": true,
    "indisponibiliteTemporaireBasculeAutoDisponible": true,
    "indisponibiliteTemporaireMailAvantIndisponibilite": true,
    "indisponibiliteTemporaireMailApresIndisponibilite": true,
    "listeNumeroPei": "PEI-001, PEI-002",
    "organismeApiMaj": "SDIS_01"
  }
]
----

=== Ajouter une indisponibilité temporaire

==== `POST /`
Ajoute une nouvelle indisponibilité temporaire.

*Droit requis :* `TRANSMETTRE`

*Structure de requête :*

Objet `ApiIndispoTempFormData` :

[source,json]
----
{
  "motif": "Travaux sur le réseau",
  "observation": "Remplacement de canalisation",
  "dateDebut": "2024-12-25T14:30:00.000Z",
  "mailAvantIndisponibilite": true,
  "mailApresIndisponibilite": true,
  "basculeAutoDisponible": true,
  "basculeAutoIndisponible": true,
  "notificationDebut": "2024-12-25T08:00:00.000Z",
  "notificationFin": "2024-12-26T18:00:00.000Z",
  "notificationResteIndispo": null,
  "dateFin": "2024-12-26T14:30:00.000Z",
  "listeNumeroPei": ["PEI-001", "PEI-002"]
}
----

*Structure de réponse :*

Objet `ApiIndispoTemporaire` créé (voir structure ci-dessus)

=== Modifier une indisponibilité temporaire

==== `PUT /{idIndisponibiliteTemporaire}`
Modifie les informations relatives à une indisponibilité temporaire.

*Paramètres de chemin :*

- `idIndisponibiliteTemporaire` (requis) : Identifiant de l'indisponibilité temporaire

*Droit requis :* `TRANSMETTRE`

*Structure de requête :*

Objet `ApiIndispoTempFormData` (voir structure ci-dessus)

*Structure de réponse :*

Objet `ApiIndispoTemporaire` mis à jour (voir structure ci-dessus)

== Documentation OpenAPI

*Base URL :* `/api/openapi`

=== Spécification OpenAPI

==== `GET /openapi.{type}`
Retourne la spécification OpenAPI de l'API.

*Paramètres de chemin :*

- `type` : Format de la spécification ('json' ou 'yaml')

*Accès :* Public (pas d'authentification requise)

=== Interface Swagger UI

==== `GET /`
Affiche l'interface Swagger UI pour explorer l'API.

*Accès :* Public (pas d'authentification requise)

== Formats de Réponse

Toutes les réponses sont au format JSON avec l'encodage UTF-8, sauf indication contraire.

*En-tête de réponse :*
[source]
----
Content-Type: application/json; charset=UTF-8
----

== Codes de Statut HTTP

- `200` : Succès
- `201` : Créé avec succès
- `400` : Erreur dans la requête
- `401` : Non authentifié
- `403` : Accès refusé (droits insuffisants)
- `404` : Ressource non trouvée
- `500` : Erreur serveur

== Notes Importantes

1. *Modèle AFIGEO* : Les données PEI sont conformes au modèle de donnée minimal défini par l'AFIGEO.
2. *Limite de résultats* : Certains endpoints ont une limite maximale (ex: 200 pour la liste des PEI).
3. *Formats de date* : Les dates sont au format `YYYY-MM-DD hh:mm`.
4. *Types de PEI* :
- **PIBI** : Points d'Eau Incendie sous pression (Poteaux et Bouches Incendie)
- **PENA** : Points d'Eau Naturels et Artificiels
5. *Natures DECI* :
- `PUBLIC` : DECI publique
- `PRIVE` : DECI privée
- `CONVENTIONNE` : DECI privée sous convention
- `ICPE` : DECI des Installations Classées pour la Protection de l’Environnement
- `ICPE_CONVENTIONNE` : DECI des Installations Classées pour la Protection de l’Environnement sous convention

== Structures de Données (Objets JSON)

Cette section décrit les structures de données attendues dans le corps des requêtes POST et PUT.

=== ApiPibiFormData

Objet utilisé pour la modification des caractéristiques techniques des PIBI (`PUT /api/deci/pei/{numeroComplet}/pibi-caracteristiques`).

[source,json]
----
{
  "codeDiametre": "string (optionnel)",
  "diametreCanalisation": "number (optionnel)",
  "peiJumele": "string (optionnel)",
  "inviolabilite": "boolean (optionnel)",
  "renversable": "boolean (optionnel)",
  "codeMarque": "string (optionnel)",
  "codeModele": "string (optionnel)",
  "anneeFabrication": "number (optionnel)",
  "codeTypeReseau": "string (optionnel)",
  "codeTypeCanalisation": "string (optionnel)",
  "reseauSurpresse": "boolean (optionnel)",
  "reseauAdditive": "boolean (optionnel)"
}
----

**Propriétés :**

* `codeDiametre` : Code du diamètre de demi-raccord
* `diametreCanalisation` : Diamètre de la canalisation en millimètres
* `peiJumele` : Numéro du PEI jumelé
* `inviolabilite` : Indique si le PEI est équipé d'un système d'inviolabilité
* `renversable` : Indique si le PEI est renversable
* `codeMarque` : Code de la marque du PIBI
* `codeModele` : Code du modèle du PIBI
* `anneeFabrication` : Année de fabrication
* `codeTypeReseau` : Code du type de réseau
* `codeTypeCanalisation` : Code du type de canalisation
* `reseauSurpresse` : Indique si le réseau est surpressé
* `reseauAdditive` : Indique si le réseau utilise des additifs

=== ApiPenaFormData

Objet utilisé pour la modification des caractéristiques techniques des PENA (`PUT /api/deci/pei/{numeroComplet}/pena-caracteristiques`).

[source,json]
----
{
  "capaciteIllimitee": "boolean (optionnel)",
  "capaciteIncertaine": "boolean (optionnel)",
  "capacite": "number (optionnel)",
  "quantiteAppoint": "number (optionnel)",
  "codeMateriau": "string (optionnel)",
  "equipeHBE": "boolean (optionnel)"
}
----

**Propriétés :**

* `capaciteIllimitee` : Indique si la capacité est illimitée
* `capaciteIncertaine` : Indique si la capacité est incertaine
* `capacite` : Capacité en litres
* `quantiteAppoint` : Quantité d'appoint en litres
* `codeMateriau` : Code du matériau du PENA
* `equipeHBE` : Indique si équipé d'un système HBE (Hélicoptère Bombardier d'Eau)

=== ApiVisiteFormData

Objet utilisé pour l'ajout et la modification de visites (`POST` et `PUT /api/deci/pei/{numeroComplet}/visites`).

[source,json]
----
{
  "date": "string (requis)",
  "typeVisite": "string (requis)",
  "agent1": "string (optionnel)",
  "agent2": "string (optionnel)",
  "anomaliesControlees": ["string", "string"],
  "anomaliesConstatees": ["string", "string"],
  "debit": "number (optionnel)",
  "debitMax": "number (optionnel)",
  "pression": "number (optionnel)",
  "pressionDynamique": "number (optionnel)",
  "pressionDynamiqueDebitMax": "number (optionnel)",
  "observations": "string (optionnel)"
}
----

**Propriétés :**

* `date` : Date de la visite au format ISO (YYYY-MM-DD)
* `typeVisite` : Code du type de visite
* `agent1` : Nom du premier agent
* `agent2` : Nom du second agent (optionnel)
* `anomaliesControlees` : Liste des codes d'anomalies contrôlées
* `anomaliesConstatees` : Liste des codes d'anomalies constatées
* `debit` : Débit mesuré en litres/minute
* `debitMax` : Débit maximal mesuré en litres/minute
* `pression` : Pression statique en bars
* `pressionDynamique` : Pression dynamique en bars
* `pressionDynamiqueDebitMax` : Pression dynamique au débit maximal en bars
* `observations` : Observations textuelles

=== ApiIndispoTempFormData

Objet utilisé pour l'ajout et la modification d'indisponibilités temporaires (`POST` et `PUT /api/deci/indispoTemporaire`).

[source,json]
----
{
  "motif": "string (requis)",
  "observation": "string (optionnel)",
  "dateDebut": "string (requis, format ISO 8601)",
  "mailAvantIndisponibilite": "boolean (défaut: true)",
  "mailApresIndisponibilite": "boolean (défaut: true)",
  "basculeAutoDisponible": "boolean (défaut: true)",
  "basculeAutoIndisponible": "boolean (défaut: true)",
  "notificationDebut": "string (optionnel, format ISO 8601)",
  "notificationFin": "string (optionnel, format ISO 8601)",
  "notificationResteIndispo": "string (optionnel, format ISO 8601)",
  "dateFin": "string (optionnel, format ISO 8601)",
  "listeNumeroPei": ["string", "string"]
}
----

**Propriétés :**

* `motif` : Motif de l'indisponibilité
* `observation` : Observations complémentaires
* `dateDebut` : Date et heure de début d'indisponibilité
* `mailAvantIndisponibilite` : Envoi d'un mail avant l'indisponibilité
* `mailApresIndisponibilite` : Envoi d'un mail après l'indisponibilité
* `basculeAutoDisponible` : Basculement automatique en disponible
* `basculeAutoIndisponible` : Basculement automatique en indisponible
* `notificationDebut` : Date/heure de notification de début
* `notificationFin` : Date/heure de notification de fin
* `notificationResteIndispo` : Date/heure de notification si reste indisponible
* `dateFin` : Date et heure de fin d'indisponibilité
* `listeNumeroPei` : Liste des numéros complets des PEI concernés

=== Formats de Date

Les dates doivent être au format ISO 8601 :

* **Date simple** : `YYYY-MM-DD` (ex: `2024-12-25`)
* **Date et heure avec fuseau** : `YYYY-MM-DDTHH:mm:ss.sssZ` (ex: `2024-12-25T14:30:00.000Z`)

=== Validation

* Les champs marqués comme **requis** doivent obligatoirement être fournis
* Les valeurs numériques doivent respecter les contraintes métier (ex: débits positifs)
* Les codes de référentiel doivent exister dans la base de données
* Les dates de fin doivent être postérieures aux dates de début
